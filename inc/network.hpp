#ifndef IG_NETWORK_HPP
#define IG_NETWORK_HPP

#include "layer.hpp"

namespace hoge {
    class network {
        std::vector<layer<double, double> *> ls;

    public:
        network() { }
    };
}

#endif

